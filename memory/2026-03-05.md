- **14:25 PM:** User approved the `gog` skill. This will be installed along with `github` and `find-skills` after the rate-limit cooldown. This skill is particularly important as it may unblock the automated email reports.
- **10:45 AM:** OpenClaw reliability fix: cron/subagent announce deliveries were defaulting to `to=heartbeat` (Telegram `@heartbeat`) and failing. Patched all `delivery.mode=announce` cron jobs to deliver to Telegram chat id `6736310070` with `bestEffort=true` and failure alerts. Also set `channels.telegram.allowFrom` + `channels.telegram.groupAllowFrom` to `[6736310070]` (fixes group allowlist drop) and restarted `ai.openclaw.gateway`.
- **17:35 PM:** Self-improve: reduced Telegram DM noise by changing hourly `healthcheck-scout-isolated` + `Self-improvement loop` cron deliveries to `mode=none` (silent unless failure alert). Memory hardening: set `agents.defaults.memorySearch.provider=local`, removed gemini fallback, and reindexed memory for all agents so each store is `27/27 files` with consistent `Vector dims: 768`.
- **18:09 PM:** Memory upgrade: added curated `memorySearch.extraPaths` (core workspace pointers + Endless Molt GTM/SEO docs) and reindexed all agents so each store is `51/51 files` (305 chunks).
