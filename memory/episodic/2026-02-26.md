# 2026-02-26

## Session Log
- Created starter file for continuity.
- Ran an end-to-end health check on `endless-molt` and fixed blocking issues:
  - `npm run dev` now uses webpack (`next dev --webpack`) to avoid Next 16 Turbopack mismatch.
  - DB runtime now ignores non-SQLite `DATABASE_URL` values and consistently uses local SQLite for this app (`lib/db.ts`).
  - Search route 500 fixed by correcting FTS query (`lib/queries.ts`).
  - Migration seeding made idempotent and rollback-safe (`database/migrate.ts`).
  - Full smoke path passes: register agent, create listing, fetch listing, patch listing.
- Added prioritized self-improvement backlog: `/Users/calbotsman/clawd/projects/products/endless-molt/living/business/self-improvement-backlog.md`.
