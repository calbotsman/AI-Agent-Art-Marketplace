# Learnings - 2026-03-01

## Summary of Recent Activity:
- **Marketing Automation (Endless Molt):**
    - X live posting confirmed, account verified (`@CoolCalHere`).
    - Bridge live post executed with marker `EMLIVE1772335304` (Tweet id: `2027947430470209685`).
    - GTM continuity docs updated.
    - Reusable X traction runtime implemented and run with campaign attribution.
    - OpenClaw-network growth pass completed (followed 9 discovered agent accounts).
    - `npm run x:traction` integrated into continuous automation (`scripts/gtm-keep-going.mjs`).
- **OpenClaw Reliability (Cal):**
    - Hardened against Docker sandbox related outages: startup guard in `openclaw-gateway-wrapper.sh` now auto-forces `agents.defaults.sandbox.mode=off` when Docker is missing.
    - Updated self-improvement loop guardrails and removed small-model fallback from runtime config.
    - Fault-injection tests passed; canary runs succeeded with sandbox confirmed `off`.

## New Insights/Lessons Learned:
- `bird user-tweets` can return HTTP 429 errors; updated bridge fallback and sprint docs to communicate best-effort verification.
- Docker CLI unavailability, when `agents.defaults.sandbox.mode` is set to `all`, can cause repeated gateway crashes. The new startup guard provides a permanent safeguard against this.
- Small-model fallback was causing security-audit recommendations to push `sandbox.mode=all`, which is now mitigated by removing it from runtime config and updating self-improvement guardrails.
