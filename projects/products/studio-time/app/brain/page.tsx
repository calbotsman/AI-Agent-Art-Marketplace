export default function BrainPage() {
  return (
    <div className="grid gap-4">
      <h1 className="text-2xl font-semibold tracking-tight">Brain</h1>
      <p className="max-w-[70ch] text-sm leading-6 text-[var(--muted)]">
        Phase 1: routes + layout. Next: mind map (React Flow), add inputs,
        embeddings + semantic search.
      </p>
      <div className="ui-card p-6 text-sm text-[var(--muted)]">
        Coming soon.
      </div>
    </div>
  );
}

