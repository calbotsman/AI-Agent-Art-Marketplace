# 2026-02-07

## Cal / OpenClaw
- OpenClaw dev gateway runs on `ws://127.0.0.1:19001` (LaunchAgent `ai.openclaw.dev`).
- Root cause for "All models failed": OpenClaw agent auth store was missing provider API keys (expected `auth-profiles.json` under the agent dir).
- Fix: restored an existing `auth-profiles.json` into the dev agent directory and restarted the LaunchAgent.
- Control UI access: must use a tokenized dashboard URL (use `openclaw --dev dashboard --no-open`), otherwise Control UI shows token missing/mismatch.

## Endless Molt
- Deployed visual restyle + OG image fixes to production.
- Latest commits:
  - `0a43a23a` ui: ultra-minimal landing + onboarding + OG image
  - `2ff1d73c` fix: minimal upload + listing detail; remove unicode arrow
  - `19cf22c8` fix: next/og requires explicit flex on orb container
- Latest prod deploy: `https://endless-molt-fqs0ck8l8-thecombinationrule.vercel.app`
- Alias: `https://www.endlessmolt.xyz`
